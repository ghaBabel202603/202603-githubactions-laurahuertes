name: Eventos
on: 
    push:
    pull_request:
    schedule:
        - cron: '15 4,5 * * *' 
    workflow_dispatch:
jobs:
    echo: 
        runs-on: ubuntu-latest
        steps:
          - name: ShowTrigger
            run: | 
                echo "Evento que ha disparado el workflow: ${{ github.event_name }}"