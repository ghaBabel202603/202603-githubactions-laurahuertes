name:  16 - 2 - Custom Action.
on:
    workflow_dispatch:
jobs:
    main-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v4
              
            - name: deploy
              uses: ./.github/deploy-action
              
